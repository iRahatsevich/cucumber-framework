package application;

import org.junit.Assert;
import org.junit.Test;

import static org.junit.Assert.assertEquals; // can import this static method and dont need tohave Assert. infront every test
/*
public class BankerDemoTest {
    BankOfAmerica boa = new BankOfAmerica();
    Chase chase = new Chase();

    @Test
    public void transferFundsTest() {
        BankerDemo.transferFunds(boa , chase, 200);
        assertEquals(0.0, boa.getBalance(),0);
        assertEquals(200, chase.getBalance(),0);

    }
    @Test
    public void insufficientFundsTransfer(){
        BankerDemo.transferFunds(boa , chase , 500);
        assertEquals(200, boa.getBalance(), 0);
        assertEquals(0.0, chase.getBalance(), 0);
    }
}
*/